spring.application.name=pethotelgo

# JWT Configuration - DO NOT COMMIT SECRETS TO REPO
# In production set JWT_SECRET as a base64-encoded 512-bit key in environment.
# For local development, set JWT_SECRET in your environment or use .env (not committed).
jwt.secret=${JWT_SECRET:}
jwt.expiration=3600000
jwt.refresh.expiration=604800000

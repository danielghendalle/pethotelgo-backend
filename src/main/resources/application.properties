spring.application.name=pethotelgo

# Firebase Configuration
# Option 1: Set firebase.credentials.path to path of service account JSON key
firebase.credentials.path=
# Option 2: Use Application Default Credentials (ADC) - set GOOGLE_APPLICATION_CREDENTIALS env var
firebase.use_adc=true

